Steps to Publish a package (each script provides command line feedback on usage):

1) run create_package.ps1 to create the nuget package

2) run publish_package.ps1 to publish the package to the publish server